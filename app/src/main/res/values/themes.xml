<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="ClothsApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/g_black</item>
        <item name="colorPrimaryVariant">@color/g_navy</item>
        <item name="colorOnPrimary">@color/g_brown</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="Theme.ClothsApp" parent="ClothsApp" />
</resources>